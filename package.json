{
  "name": "amazon-smart-recommendations-api",
  "version": "1.0.0",
  "description": "API for Amazon Smart Recommendations Chrome Extension",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.5",
    "axios": "^1.6.0",
    "node-fetch": "^2.6.9"
  }
}
